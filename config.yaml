data:
  image_size: 28
  num_classes: 10

training:
  batch_size: 32
  num_epochs: 10
  learning_rate: 0.001
  random_seed: 42
  optimizer: "adam"

paths:
  models_dir: outputs/models
  figures_dir: outputs/figures